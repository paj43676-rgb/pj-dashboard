name: legacy-update-cache-data

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Explain legacy status
        run: |
          echo "This workflow is kept only for manual fallback."
          echo "Scheduled live refresh now uses .github/workflows/deploy-pages.yml"
          echo "Leaving this job manual-only prevents repeated failure noise in Actions."
